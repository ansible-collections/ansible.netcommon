---
bugfixes:
  - Temporarily disabled direct module execution in `network.py` action plugin for compatibility with ansible-core 2.19+.
    Prevents errors like `No start of json char found` and `ActionBase._parse_returned_data() missing 1 required positional argument 'profile'`.
